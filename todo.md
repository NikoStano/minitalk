## MINITALK TO_DO LIST

# NOTIONS
- [ ] check comment marche les signaux`SIGUSR1` `SIGUSR2`

# MANDATORY
- [ ] creer l'exec server et au lancement affiche le `PID` dans le `TERMINAL` initial
- [ ] le `CLIENT` prend 2 args `PID` `STR`
- [ ] le `CLIENT` transmet au `SERVER` avec le `PID` une `STR` et le server doit l'afficher (`RAPIDEMENT`)
- [ ] `PLUSIEURS CLIENT` peuvent communiquer avec un server
- [ ] Utiliser uniquement les signaux `UNIX`  `SIGUSR1` et `SIGUSR2`

# BONUS
- [ ] `SERVER` confirme la reception avec un signal au `CLIENT` 
- [ ] Gestion des caractere `UNICODE`